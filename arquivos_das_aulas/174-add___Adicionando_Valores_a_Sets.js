//add
/* 
    A função add() é uma função que deve ser usada em elementos que são sets - conjuntos de valores iteráveis como os arrays - para adicionar valores aos sets.

    Podemos usar também junto a alguns elementos que geram sets, como o "classList" por exemplo, que gera um set com os nomes das classes de um elemento, podendo adicionar uma nova classe a um elemento. 

    Podemos ver isso no exemplo abaixo...
*/

document.querySelector('div').classList.add('nova_classe')

console.log(document.querySelector('div').classList)